name: playground
description: Play knows no limit

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  firebase_auth: ^1.0.2
  firebase_dynamic_links: ^0.8.0
  provider: ^5.0.0
  firebase_core: ^1.0.3
  google_sign_in: ^5.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/
